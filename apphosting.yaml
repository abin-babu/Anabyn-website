# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

# Specifies the directory that contains your application source code.
sourceDirectory: .

runConfig:
  # Increase this value to automatically spin up more instances in response to increased traffic.
  maxInstances: 1
  # Minimum number of CPU cores and memory for each instance.
  cpu: 1
  memoryMiB: 512
