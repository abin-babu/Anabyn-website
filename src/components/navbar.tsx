
'use client';

import { Header } from './layout/header';

/**
 * Navbar component serving as a wrapper for the premium sticky header.
 * Centralizes navigation logic for the entire application.
 */
export function Navbar() {
  return <Header />;
}
