
{
  "entities": {
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product in the Anabyn Premium Gallery.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "name": {
          "type": "string",
          "description": "The name of the product."
        },
        "slug": {
          "type": "string",
          "description": "The URL-friendly slug for the product."
        },
        "category": {
          "type": "string",
          "description": "The category of the product."
        },
        "subcategory": {
          "type": "string",
          "description": "The subcategory of the product."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the product."
        },
        "images": {
          "type": "array",
          "description": "An array of image URLs for the product.",
          "items": {
            "type": "string"
          }
        },
        "specs": {
          "type": "object",
          "properties": {
            "size": {
              "type": "string"
            },
            "GSM": {
              "type": "string"
            },
            "materials": {
              "type": "string"
            }
          }
        },
        "moq": {
          "type": "string",
          "description": "Minimum Order Quantity for the product."
        },
        "leadTime": {
          "type": "string",
          "description": "The lead time for the product."
        },
        "tags": {
          "type": "array",
          "description": "An array of tags associated with the product.",
          "items": {
            "type": "string"
          }
        },
        "published": {
          "type": "boolean",
          "description": "Indicates whether the product is published or not."
        },
        "createdAt": {
          "type": "string",
          "description": "The date and time the product was created.",
          "format": "date-time"
        }
      },
      "required": [
        "name",
        "slug",
        "category",
        "description",
        "images",
        "specs",
        "moq",
        "leadTime",
        "published",
        "createdAt"
      ]
    },
    "Inquiry": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Inquiry",
      "type": "object",
      "description": "Represents an inquiry made by a customer.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the person making the inquiry."
        },
        "company": {
          "type": "string",
          "description": "The company of the person making the inquiry."
        },
        "email": {
          "type": "string",
          "description": "The email address of the person making the inquiry.",
          "format": "email"
        },
        "phone": {
          "type": "string",
          "description": "The phone number of the person making the inquiry."
        },
        "whatsappOptIn": {
          "type": "boolean",
          "description": "Indicates whether the person opted in to WhatsApp communication."
        },
        "message": {
          "type": "string",
          "description": "The message included in the inquiry."
        },
        "products": {
          "type": "array",
          "description": "An array of product IDs and quantities included in the inquiry.",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "qty": { "type": "number" }
            }
          }
        },
        "type": {
          "type": "string",
          "description": "The type of inquiry (sample, general, bulk)."
        },
        "status": {
          "type": "string",
          "description": "The status of the inquiry (new, contacted, quoted, done)."
        },
        "source": {
          "type": "string",
          "description": "The source of the inquiry (site, whatsapp)."
        },
        "createdAt": {
          "type": "string",
          "description": "The date and time the inquiry was created.",
          "format": "date-time"
        }
      },
      "required": [
        "name",
        "email",
        "message",
        "products",
        "type",
        "status",
        "source",
        "createdAt"
      ]
    },
    "Admin": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Admin",
      "type": "object",
      "description": "Represents an admin user with access to the dashboard.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Admin entity. This matches the Firebase Auth UID."
        },
        "name": {
          "type": "string",
          "description": "The name of the admin user."
        },
        "email": {
          "type": "string",
          "description": "The email address of the admin user.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The role of the admin user."
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "role"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": {
            "$ref": "#/entities/Product"
          },
          "description": "Stores product information. Each document represents a single product."
        }
      },
      {
        "path": "/inquiries/{inquiryId}",
        "definition": {
          "entityName": "Inquiry",
          "schema": {
            "$ref": "#/entities/Inquiry"
          },
          "description": "Stores customer inquiries. Each document represents a single inquiry."
        }
      },
      {
        "path": "/admin/{uid}",
        "definition": {
          "entityName": "Admin",
          "schema": {
            "$ref": "#/entities/Admin"
          },
          "description": "Stores admin user profiles. The document ID is the Firebase Auth UID."
        }
      }
    ]
  }
}
